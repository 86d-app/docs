---
title: "Abandoned carts"
description: "Recover abandoned carts in 86d with scheduled reminder emails. Detects carts left idle and triggers a multi-step recovery sequence."
---

The abandoned-carts module identifies carts that have been idle past a threshold and triggers a configurable reminder sequence. With `@86d-app/notifications` and a `RESEND_API_KEY` configured, reminders are sent by email. The module exposes admin tools to inspect recovery rates and override per-cart sequences.

**Source:** [`modules/abandoned-carts`](https://github.com/86d-app/86d/tree/main/modules/abandoned-carts) · **npm:** [`@86d-app/abandoned-carts`](https://www.npmjs.com/package/@86d-app/abandoned-carts)

## Installation

```sh
86d module add abandoned-carts
```

## Configuration

```ts
abandonedCarts({
  abandonAfterMinutes: "60",
  reminderSchedule: "60,1440,4320",   // minutes after abandonment: 1h, 24h, 72h
});
```

## Admin endpoints

| Method | Path | Description |
| --- | --- | --- |
| `GET` | `/admin/abandoned-carts` | List abandoned carts |
| `POST` | `/admin/abandoned-carts/:id/send-now` | Send the next reminder immediately |

## Related modules

- [Cart](/modules/cart), [Notifications](/modules/notifications), [Newsletter](/modules/newsletter).
